import React, { useEffect } from 'react';

const CV = () => {
    useEffect(() => {
        document.title = `Joel Perren · CV`;
    });

    return (
        <main>
            <p>CV</p>
        </main>
    );
}

export default CV;