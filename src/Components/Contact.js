import React, { useEffect } from 'react';

const Contact = () => {
    useEffect(() => {
        document.title = `Joel Perren · Contact`;
    });

    return (
        <main>
            <p>Contact</p>
        </main>
    );
}

export default Contact;